import 'package:flutter/cupertino.dart';

class NewComp extends StatelessWidget {
  const NewComp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
